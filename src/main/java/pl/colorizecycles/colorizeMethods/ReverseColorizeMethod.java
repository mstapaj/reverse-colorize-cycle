package pl.colorizecycles.colorizeMethods;

public interface ReverseColorizeMethod {
    ColorizeResult reverseColorize(int cycleSize, int expectedIteration, boolean reduceLastNumbers, int maxNumber);
}
